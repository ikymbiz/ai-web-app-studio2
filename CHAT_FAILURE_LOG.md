# このチャットの失敗ログ

作成日: 2026-05-23  
対象: AI Web Studio Pro 修正作業

## 目的

このログは、今回のチャット内で発生した失敗・未達・再発防止策を明文化し、今後の修正時に同じ問題を繰り返さないための記録です。

## 失敗一覧

| No | 失敗内容 | 影響 | 原因 | 今回の対応 | 状態 |
|---:|---|---|---|---|---|
| 1 | 「修正した」と説明した内容が、実コードに反映されていなかった | ユーザーが再確認・再指摘する必要が発生 | 説明だけで終わり、実ファイル差分と検証を作らなかった | 実ファイル `index.html` / `prompts.json` を直接修正し、構文検証を実施 | 対応済み |
| 2 | AI応答内の複数質問を、質問ごとではなくフラットな選択肢として扱っていた | 「選択肢を開く（8）」のように表示され、問いごとの回答ができなかった | 選択肢抽出処理が `parseOptionsFromText()` の単純な箇条書き抽出のみだった | `parseOptionGroupsFromText()` を追加し、質問グループごとのステップ式モーダルに変更 | 対応済み |
| 3 | システムプロンプトの品質が下がり、削除禁止・省略禁止・検証義務が弱かった | AIが勝手に機能を削除・省略・解釈するリスクが高まった | プロンプトが一般的な開発者指示に寄りすぎ、今回の運用上の禁止事項が明文化されていなかった | `prompts.json` と `index.html` 内fallbackの両方を、厳格差分実装・削除禁止・検証必須の内容に同期 | 対応済み |
| 4 | ユーザーの「ペルソナは要件ではない」という指摘が十分に反映されていなかった | ペルソナに引きずられた仕様提案・実装になる可能性 | PLANプロンプト内でペルソナの扱いが曖昧だった | PLAN/SUGGESTプロンプトで「ペルソナは参考情報であり要件として固定しない」と明記 | 対応済み |
| 5 | 既存機能を勝手に削除・省略するなという指示への防御が弱かった | APIキー管理、GitHub PAT、デプロイ、言語切替、タスク管理などの退行リスク | verifyプロンプトが「漏れ」だけを見ており、退行や削除を検出対象にしていなかった | verifyプロンプトに「既存機能の削除・省略・ID変更・保存形式変更・UI導線消失も漏れ」と明記 | 対応済み |
| 6 | 選択肢モーダルがスマホで見切れやすかった | Android Chromeの狭幅・キーボード表示時に操作しづらい | モーダル内の高さ制御と下部アクション固定が弱かった | `max-height: calc(100dvh - 32px)`、スクロール、sticky footer、48px以上の選択ボタンを追加 | 対応済み |
| 7 | JSONプロンプト管理の利点を活かしきれていなかった | `prompts.json` とfallbackの内容がズレるリスク | 外部JSONを読む実装はあるが、fallbackの同期ルールが弱かった | `prompts.json` を正とし、`index.html` のfallbackも同じ内容へ更新 | 対応済み |

## 今回追加・修正した主なファイル

- `index.html`
  - 質問グループ抽出 `parseOptionGroupsFromText()` を追加。
  - 選択肢モーダルを1問ずつ進むステップ式に変更。
  - 「その他」自由入力、戻る、複数選択、回答反映を実装。
  - ハードコードfallbackプロンプトを厳格版に更新。
- `prompts.json`
  - バージョンを `3.2-strict-json-step-modal` に更新。
  - system / plan / suggest / debug / extract / verify を厳格化。
- `sw.js`
  - キャッシュ名を更新し、古いUIが残る可能性を下げた。
- `CHAT_FAILURE_LOG.md`
  - このチャットの失敗と再発防止策を記録。
- `README.md`
  - 今回の修正内容を追記。
- `VERIFICATION_REPORT.md`
  - 今回の検証結果を追記。

## 再発防止ルール

1. 修正依頼では、説明だけで終わらせず、必ずファイルを修正する。
2. 「修正済み」と言う前に、変更ファイル・検証内容・未確認事項を確認する。
3. 既存機能は、ユーザーが明示的に削除を指示しない限り保持する。
4. 複数質問と複数選択肢は、フラット配列ではなく質問グループとして扱う。
5. `prompts.json` を正とし、ローカル起動時のfallbackも同期する。
6. 仕様検証では「新規要件の漏れ」だけでなく「既存機能の退行」も検出する。

## 検証メモ

- `index.html` 内の `<script>` を抽出し、JavaScript構文検証を実施。
- `prompts.json` を JSON として検証。
- 実例のAI応答を使い、次の3グループに分かれることを確認。
  1. ターゲットユーザー
  2. 主要機能
  3. 特に重視したい点
- 主要機能の選択肢で `履歴機能` と `メモ機能` が質問見出し扱いで除外されないことを確認。

## 未確認事項

- 実機Android Chromeでのタップ操作、キーボード表示、Service Worker更新は、この環境では実機検証できていない。
- GitHub APIの実デプロイは、PAT権限とリポジトリ状態に依存するため、この環境では静的確認まで。

## 2026-05-25 v29.3：ペルソナ先行 / MVP直接生成 / UIレビューモーダル廃止

### 指摘内容（ユーザー要求 5項目）

1. 最初にターゲットペルソナを確認し、ペルソナをセットすること。
2. いくつかのユーザビリティの分岐に関する質問をして生成させるアプリの方向性を決めること。
3. 基本的な機能やよしなにしても問題ない内容は質問しないこと。
4. UIを先に決めたいが、最初の出力は MVP で実際に動くものであること。
5. UIデザインの確認モーダルは廃止。

### 対応

- `prompts.json` の `plan` プロンプトを「ペルソナ未設定→ `id="persona"` 質問必須」「以降は UI/UX 分岐のみ」「基本機能（CRUD/保存/ログイン/言語/テーマ/PWA/エラー処理/空状態/最小ドキュメント）は質問せず既定で含める」「最初の出力は実際に動く MVP 前提」に書き換え。`_version` を `v29-3-persona-first-mvp` に更新。
- `index.html` の `buildRequirementFlowInstruction()` をペルソナ有無で分岐させ、未設定時のみ `id="persona"` 質問を強制。
- `applyOptionsModalAnswers()` に「`id="persona"` または質問文がペルソナ系キーワードに一致する場合、回答を `target-persona` 欄に自動保存し、`updatePromptPreviews()`／`autoSaveSettings()` を呼ぶ」処理を追加。ペルソナ回答は AI へ送るメッセージ本文からは除外し、再確認不要であることを AI に伝える。
- 「⚡️ コードを作成」ボタンの `onclick` を `generateUiFirstReview()` → `generateCode()` に変更（フローティングボタン と チャット内動的ボタンの2か所）。
- `ui-review-modal` の HTML ブロックを削除。
- UI 先行レビュー関連関数（`generateUiFirstReview`／`generateCoreLogicFromApprovedUi`／`showUiReviewModal`／`closeUiReviewModal`／`approveUiReviewAndGenerateCore`／`requestUiRevisionFromReview`／`buildUiReviewSummary`／`saveUiFirstReviewState`／`getUiFirstReviewState`）を no-op スタブまたは `generateCode()` への薄いエイリアスに置換。互換性のため関数自体は残す。
- `runPreviewDebugRequest()` から `isUiReviewRevision` 分岐を削除し、通常のデバッグ修正フローに統一。
- `sw.js` の `CACHE_NAME` をバンプ。
- `README.md` に変更内容を追記。

### 維持した既存機能（削除していない項目）

- APIキー管理、GitHub PAT、デプロイ、保存先設定、テーマ、テンプレート、プロンプトテンプレート編集。
- 検証 AI、原因分析、自動修正フロー、進行ログ、`HANDOVER` 永続化。
- 検証完了後の「プレビュー前レビュー」モーダル（`preview-review-modal`）。今回廃止したのは UI 先行レビューモーダル（`ui-review-modal`）のみ。
- `uiFirstGenerateButton` などの i18n ラベルは未使用化したが、削除はせず残置（運用ルール「未指示の削除禁止」に従う）。
- `summarizeUiSurface()` は永続化された workflow state からの参照に備えて軽量な後方互換実装として残す。

### 再発防止

- ユーザーが「ペルソナをセットすること」と指示した場合、AI 回答からの抽出だけに任せず、UI から確実に保存される経路（モーダル → 入力欄）を実装する。
- 「最初の出力は MVP」のような要件は、プロンプト文言の書き換えだけでなく、実装上「どの関数が呼ばれるか」をボタン onclick まで遡って確認する。
- モーダル廃止指示では、HTML だけでなく関連関数の参照箇所（呼び出し側）まで合わせて削除または無害化する。互換性のため即削除せず、no-op スタブを残す方針を採用。



### 指摘内容

- ZIPファイルや複数ファイルの添付ができるようにする必要があった。
- 設定画面を用途別に分ける必要があった。
- 管理用システムプロンプトにプリンシプル、システム要件、実装検証用の欄が必要だった。
- 管理用Skill設定画面が必要だった。

### 対応

- v3.3で複数添付配列、ZIP読込、コードZIP展開、設定画面分割、管理用追加欄、Skill設定欄を追加。
- 既存のAPIキー管理、GitHub PAT、デプロイ、保存先、テーマ、テンプレート、プロンプトテンプレートは削除せず保持。

### 再発防止

- 設定画面の追加・分割時は、既存設定項目の移動先を明示し、削除ではなく再配置として扱う。
- 添付仕様は「単一ファイル」「複数ファイル」「ZIP」「画像」「テキスト」「バイナリ」を分けて検証する。
